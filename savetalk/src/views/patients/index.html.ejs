<% patients.forEach( patient => { %>
    <div>
        <h2><%= patient.name %></h2>
        <p><%= patient.city %></p>
        <a href="<%= patientPath(patient.id) %>">Ver más</a>
    </div>
<% }) %>