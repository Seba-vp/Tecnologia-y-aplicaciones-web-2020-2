<html>

<head>
    <link rel="stylesheet" href="src/assets/styles/plantilla1.scss">
</head>

<body>

    <div class="header">
        <h1>Nombre: <%= patient.name %> </h1>
        <h5>Edad: <%= patient.age %> años</h5>
        <p>Cantidad de contactos: <%= chats.length %> </p>
    </div>

    <div class="caja2">
        <h2><%= pains.length %> dolores presentados </h2>
        <% if (pains.length){ %>
        <ul>
            <% pains.forEach((pain) => { %>
            <li>
                <h4><%= pain.name %></h4>
                <a href="<%= specificPainPath(pain.id) %>">Ver más info</a>
            </li>
            <% });%>
        </ul>
        <% } %>
    </div>

    <div class="caja2">
        <p><a href="<%= createPainPath(patient.id) %>"> Crear dolor </a></p>
        <br>
        <p><a href="<%= seeChatPath(patient.id) %>"> Ver contactos </a></p>
        <br>
        <p><a href="<%= updatePatientPath(patient.id) %>"> Modificar Paciente </a></p>
        <br>
        <p><a href="<%= deletePatientPath(patient.id) %>"> Borrar Paciente </a></p>
    </div>

    <div class="pie">
        <p>Made with &#10084;&#65039; by Rodolfo Mendoza Jorge Toro & Sebastian Villablanca.</p>
    </div>
</body>

</html>