<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>DentList</title>
    <% if (env !== 'development') { %>
      <link rel="stylesheet" href="<%= assetPath('app.css') %>">
    <% } %>
  </head>
  <main>
  <body>
    <header>
      <section class="status">
        <div class="topnav">
        <% if (locals.currentPatient) { %>

          <a class="noHover"></a>>Estas conectado como PACIENTEcon el mail: <%= currentPatient.email %></a>
          <a class="noHover"></a>>Estas conectado como PACIENTE con el nombre: <%= currentPatient.name %></a>
          
          <form action="<%= destroyPatientSessionPath %>" method="post">
            <input type="hidden" name="_method" value="delete" />
            <input style="float:right" type="submit" value="Cerrar Sesión" />
          </form>
        
          <% } else if (locals.currentDentist) { %>
            <a class="noHover"></a>>Estas conectado como DENTISTA con el email: <%= currentDentist.mail %></a>
            <a class="noHover"></a>>Estas conectado como DENTISTA con el nombre: <%= currentDentist.name %></a>
            <form action="<%= destroyDentistSessionPath %>" method="post">
              <input type="hidden" name="_method" value="delete" />
              <input style="float:right" type="submit" value="Cerrar Sesión" />
            </form>
        <% } else { %>

          
                <a class="noHover">NO ESTAS CONECTADO</a>
            
                <a href="<%= wantToConnectSessionPath %>" style="float:right">HAZ CLICK AQUI SI QUIERES CONECTARTE</a>
              
      

        <% } %>
         </div>


    </section>
    </header>
    <%- body %>
    <script src="<%= assetPath('app.js') %>"></script>
  </body>
</main>

<div class="footer">
  <p>Made with &#10084;&#65039; by Rodolfo Mendoza Jorge Toro & Sebastian Villablanca.</p>
</div>

</html>
