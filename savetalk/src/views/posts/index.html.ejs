
    <header>
        <h1>Posts</h1>
        <p> Aca en posts, podras ver todos los posts de voluntariados y noticias relacionadas a dentlist...</p>
    <header>
    
<div class="card">
    <h2>Agregar un Post </h2>
  <a href="<%=newPostPath %>">Agregar un post </a>
</div>

<% posts.forEach( post => { %>
    <div class="card"> 
        
        <p>ID:  <%= post.id %></p>
        <h2><%= post.title %> </h2>
        <p><%= post.description %></p>
        
     <a href="<%= postPath(post.id) %>">Ver más</a>
    </div>
<% }) %>
