<form method="post" action="<%= createChatPath %>">
    <% if (locals.errors){ %>
    <div class="card2">
        <p>No se pudo crear la conexión debido a lo siguiente:</p>
        <ul>
            <% errors.forEach(error => { %>
            <li><%= error.path %>: <%= error.message %> </li>
            <% }); %>
        </ul>
    </div>
    <% } %>
    <tabla>
    <div class="field">
        <tr>
        <td><label for="dentistId">id odontologo</label></td>
        <td><input type="text" name="dentistId" id="dentistId" value="<%= chat.dentistId%>" /></td>
    </tr>
    </div>
    <div class="field">
        <tr>
            <td><label for="patientId">id paciente</label></td>
            <td><input type="text" name="patientId" id="patientId" value="<%= chat.patientId%>" /></td>
    </tr>
    </div>

    </tabla>
    <div class="card2"> 
    <div class="actions">
        <input type="submit" value="crear contacto" />
    </div>
</div>

</form>